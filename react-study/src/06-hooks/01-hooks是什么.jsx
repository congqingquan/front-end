/**
 * 1. 在 React 中，任何以 "use" 开头的函数都被称为 hooks，即 hooks 就是表示着 use 功能函数的集合，也即是钩子函数的集合。
 * 2. hooks 可以分为三大类：
 *  1) 内置 hooks (如：useState，https://react.dev/reference/react/hooks)
 *  2) 自定义 hooks
 *  3) 三方 hooks
 */

const App = () => {
    return (
        <div></div>
    );
};

export default App;
